export type Product = {
    id: string
    name: string
    description: string
    price: string
    image:string
}

export type ProductFormValue = {
    name: string
    description: string
    price: string
    image:string
}